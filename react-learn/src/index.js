import React from 'react';
import ReactDOM from 'react-dom';
import App from "./App"

ReactDOM.render(<App/>, document.getElementById('root'));

// async function a() {
//   console.log( await getStudents(2,3))

// }
// getStudents().then(data=>console.log(data,123))
// a()